#include "main.h"

/**
 * mul - takes two integers and multiplies them
 * @a: first number
 * @b: second number
 * Return: the product of the two numbers
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
