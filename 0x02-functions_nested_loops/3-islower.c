#include "main.h"

/**
 * _islower - checks if
 * number is lowercase
 *
 * Return: 1 if lowercase
 */

int _islower(int c)
{
	char a;
	int c;

	if (a >= 'a' && a <= 'z')
		c = 1;
	else
		c = 0;
	return (c);
}
